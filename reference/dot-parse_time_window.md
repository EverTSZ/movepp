# Internal: Parse a human-readable time window to seconds

Internal: Parse a human-readable time window to seconds

## Usage

``` r
.parse_time_window(tw)
```

## Arguments

- tw:

  Character (e.g., `"3 weeks"`, `"1 day"`) or a `difftime`.

## Value

Numeric, number of seconds.
