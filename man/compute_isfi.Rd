% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isfi.R
\name{compute_isfi}
\alias{compute_isfi}
\title{Compute Individual Site Fidelity Index (ISFI)}
\usage{
compute_isfi(
  habitat_points,
  time_col,
  individual_col = "individual",
  cluster_col = "cluster_id",
  phase_col = NULL,
  min_years = 2L,
  ref_years = NULL,
  verbose = TRUE
)
}
\arguments{
\item{habitat_points}{An \code{sf} object with \code{individual_col},
\code{cluster_col}, and \code{time_col} columns (typically the output of
\code{\link[=dbscan_habitats]{dbscan_habitats()}} or \code{\link[=classify_phases]{classify_phases()}}).}

\item{time_col}{Column name (string) of a POSIXct/Date time column.
The year is extracted from this column.}

\item{individual_col}{Column name (string) of the individual ID
(default \code{"individual"}).}

\item{cluster_col}{Column name (string) of the temporary habitat
cluster ID (default \code{"cluster_id"}).}

\item{phase_col}{Optional column name for phase labels. If supplied,
ISFI is computed separately within each (individual, phase) pair.}

\item{min_years}{Integer; minimum number of unique years required
for a meaningful ISFI computation (default 2). Individuals with
fewer years return \code{NA} ISFI with a warning.}

\item{ref_years}{Optional. A single integer, or a named numeric vector
keyed by individual ID, giving the number of observation years to use
as the denominator \eqn{c} instead of the year span of
\code{habitat_points}. Use when the cluster set is a subset of a longer
record (e.g. nest-area fidelity within the breeding phase), so that
years in which the subset was absent still count against fidelity.
Default \code{NULL} (derive \eqn{c} from the data; unchanged behaviour).}

\item{verbose}{Logical; print progress.}
}
\value{
A data frame with one row per individual (or per
individual x phase if \code{phase_col} supplied), columns:
\itemize{
\item \code{individual}: individual ID.
\item \code{phase}: phase name (only when \code{phase_col} supplied).
\item \code{n_clusters}: unique clusters visited.
\item \code{n_years}: unique years tracked.
\item \code{sum_cluster_years}: total cluster-year occupancies
(\eqn{\sum_i n_{i,\mathrm{years}}}).
\item \code{isfi}: site fidelity index in 0..1, \code{NA} if undefined.
}
}
\description{
Quantifies inter-annual site fidelity per individual: the
probability that a given temporary habitat (cluster) revisited
across multiple years. Defined as
\deqn{\mathrm{ISFI} = \frac{\left(\sum_i n_{i,\mathrm{years}}\right) - n}{(c - 1) \cdot n}}
where \eqn{n} is the number of unique clusters,
\eqn{c} the number of unique years, and
\eqn{n_{i,\mathrm{years}}} the count of distinct years in which
cluster \eqn{i} was visited. ISFI ranges from 0 (each cluster
visited in only one year) to 1 (every cluster revisited in every
year).
}
\section{Use with \code{\link[=detect_nests]{detect_nests()}} output to derive INFI}{

To compute the Individual Nest Fidelity Index (INFI) introduced in
the manuscript Discussion, apply this same function to the
\code{nests} sf produced by \code{\link[=detect_nests]{detect_nests()}} across multi-year tracking,
passing the nest table's \code{nest_id} as the cluster column and the
nest start time as the time column. No separate function is needed.
}

\section{Use with phase labels for stratified analysis}{

Pass \code{phase_col} to compute ISFI separately within each phase
(e.g., breeding, stopover, wintering). This reproduces the
stratified analysis of the original manuscript (Figure 7).
}

\examples{
\dontrun{
# ISFI per individual across all habitats
fidelity <- compute_isfi(habitats, time_col = "time")

# Stratified by phase
fidelity_phase <- compute_isfi(phases, time_col = "time",
                               phase_col = "phase")
}

}
