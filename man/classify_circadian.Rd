% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian.R
\name{classify_circadian}
\alias{classify_circadian}
\title{Classify Tracking Points into Day / Twilight / Night}
\usage{
classify_circadian(
  points,
  elevation_col = "solar_elevation",
  day_threshold = 0,
  twilight_threshold = -6,
  verbose = TRUE
)
}
\arguments{
\item{points}{An \code{sf} object with a solar elevation column (typically
the output of \code{\link[=compute_solar_elevation]{compute_solar_elevation()}}).}

\item{elevation_col}{Column name (string) of solar elevation in
degrees (default \code{"solar_elevation"}).}

\item{day_threshold}{Numeric; angle (deg) above which a point is
classified as Day (default 0).}

\item{twilight_threshold}{Numeric; angle (deg) above which a point
is classified as Twilight (default -6, the civil-twilight bound).}

\item{verbose}{Logical; print summary.}
}
\value{
The input \code{sf} with a new \code{circadian} factor column
(levels: \code{"Day"}, \code{"Twilight"}, \code{"Night"}).
}
\description{
Partitions tracking points by solar elevation angle into three
circadian categories: \strong{Day} (sun above horizon), \strong{Twilight}
(civil twilight, sun between the two thresholds), and \strong{Night}
(sun well below horizon). The default thresholds follow the standard
definition of civil twilight (0 deg and -6 deg).
}
\examples{
\dontrun{
demo <- compute_solar_elevation(demo, time_col = "time")
demo <- classify_circadian(demo)
table(demo$circadian)
}

}
