% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_phases.R
\name{.mpi_density_valleys}
\alias{.mpi_density_valleys}
\title{Density peaks and valleys of a 1-D sample}
\usage{
.mpi_density_valleys(v, bw_adjust = 1)
}
\arguments{
\item{v}{Numeric vector.}

\item{bw_adjust}{Bandwidth multiplier for \code{\link[stats:density]{stats::density()}}.}
}
\value{
A list with \code{dd} (the density), \code{peak_x} (peak locations,
ascending), \code{valley_x} (valley locations, ascending) and
\code{valley_prom} (their fractional prominences).
}
\description{
Internal helper for \code{\link[=classify_phases]{classify_phases()}}. Computes a kernel density
estimate and returns its interior local maxima (peaks) and minima
(valleys), plus each valley's prominence -- the \strong{fractional} density
drop from the lower of its two nearest flanking peaks down to the
valley floor, \code{(lower_peak - valley) / lower_peak}. The prominence is
returned for optional ranking/flooring only; valley selection in
\code{\link[=classify_phases]{classify_phases()}} is anchored to the extreme peaks and needs no
prominence threshold.
}
\keyword{internal}
