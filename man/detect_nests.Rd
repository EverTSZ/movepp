% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/st_dbscan_nesting.R
\name{detect_nests}
\alias{detect_nests}
\title{Detect Bird Nesting Events with Stratified Spatiotemporal DBSCAN}
\usage{
detect_nests(
  points,
  time_col,
  individual_col = NULL,
  min_pts = 40L,
  search_dist = 41,
  time_window = "3 weeks",
  sigma_ratios = c(1, 0.68, 0.38),
  verbose = TRUE
)
}
\arguments{
\item{points}{An \code{sf} POINT object with high-resolution GPS fixes
during the breeding season.}

\item{time_col}{Column name (string) of a POSIXct/Date time column.}

\item{individual_col}{Column name (string) of the individual ID
(default \code{NULL} = pool all points).}

\item{min_pts}{Integer; baseline minimum points per cluster at the
2\eqn{\sigma} tier (default 40).}

\item{search_dist}{Numeric; baseline spatial radius at the
2\eqn{\sigma} tier, in CRS linear units (default 41, typical for
2 x 20m GPS error).}

\item{time_window}{Character or \code{difftime}; the temporal window
used by ST-DBSCAN (default \code{"3 weeks"}, suitable for a single
nesting attempt).}

\item{sigma_ratios}{Length-3 numeric vector of (min_pts, dist)
multipliers for the (2\eqn{\sigma}, 1\eqn{\sigma}, 0.5\eqn{\sigma})
tiers. Default \code{c(1.0, 0.68, 0.38)} for min_pts and is paired
internally with \code{c(1.0, 0.5, 0.25)} for distance.}

\item{verbose}{Logical; print progress messages.}
}
\value{
A list with two \code{sf} elements:
\itemize{
\item \code{points}: the input \code{sf}, augmented with \code{nest_id}
(\code{NA} if the point is not a nest point) and \code{near_dist}
(distance from the point to its nest centre).
\item \code{nests}: one row per detected nest, with columns
\code{nest_id}, \code{individual}, \code{n_points}, \code{start_time},
\code{end_time}, \code{mean_time}, and geometry (the mean centre).
}
}
\description{
Identifies nesting locations and hatching periods from high-resolution
tracking data using a three-tier stratified ST-DBSCAN scheme. The
detection rests on the assumption that a true nest yields GPS fixes
approximately bivariate-normally distributed around the actual nest
location: 95\% of points within \eqn{2\sigma}, 68\% within \eqn{1\sigma},
and ~38\% within \eqn{0.5\sigma} of the centre. A point is classified
as a nest point only if it survives the density threshold at all
three sigma tiers simultaneously, yielding markedly fewer false
positives than single-tier DBSCAN.
}
\section{Algorithm}{

For each individual (or pooled dataset if \code{individual_col = NULL}):
\enumerate{
\item Run ST-DBSCAN at three nested density tiers:
\itemize{
\item \strong{2\eqn{\sigma}}: \code{min_pts} x 1.0,
\code{search_dist} x 1.0.
\item \strong{1\eqn{\sigma}}: \code{min_pts} x 0.68,
\code{search_dist} x 0.5.
\item \strong{0.5\eqn{\sigma}}: \code{min_pts} x 0.38,
\code{search_dist} x 0.25.
}
\item Retain only points classified into a non-noise cluster in
\strong{all three tiers} (membership gate against false positives).
\item Assign each surviving point the cluster label of the
\strong{loosest (2\eqn{\sigma}) tier}, whose large radius collapses
one nest's entire fix cloud into a single \code{nest_id}. (Labelling by the
strictest tier would split one real nest into several sub-clusters,
producing many spurious nest records for a single nest.)
\item Compute the mean centre, first/last timestamp, and mean
timestamp of each detected nest.
}
}

\section{Coordinate units}{

\code{search_dist} is interpreted in the linear units of the input CRS.
If \code{points} is in WGS84 (EPSG:4326), distances are in degrees and
the function will warn. For accurate nest detection at scales of
tens of metres, project to a metric CRS first (e.g., a local UTM
zone) and supply \code{search_dist} in metres.
}

\examples{
\dontrun{
# Pied Avocet nesting data (project to UTM first for metric distance)
nests <- detect_nests(avocet_breeding_pts,
                      time_col = "time",
                      individual_col = "individual",
                      min_pts = 40,
                      search_dist = 41,
                      time_window = "3 weeks")
nests$nests  # mean centre + time bounds of each nest
}

}
\references{
Birant, D. & Kut, A. (2007). ST-DBSCAN: An algorithm for clustering
spatial-temporal data. \emph{Data & Knowledge Engineering} 60(1): 208-221.
\doi{10.1016/j.datak.2006.01.013}
}
