% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbscan_habitats.R
\name{dbscan_habitats}
\alias{dbscan_habitats}
\title{Delineate temporary habitats by DBSCAN at a behaviourally-derived scale}
\usage{
dbscan_habitats(x, individual_col = NULL, eps, minPts, drop_noise = TRUE)
}
\arguments{
\item{x}{An sf POINT object, typically the stationary (LL/LH) subset from
\code{\link[=balm_segmentation]{balm_segmentation()}}.}

\item{individual_col}{Name of the column identifying individuals. Clustering
is performed independently within each individual. If \code{NULL}, all rows are
treated as a single individual.}

\item{eps}{Neighbourhood radius in kilometres. \strong{Required, no default} --
it must be derived from the data, typically via \code{\link[=detect_habitat_params]{detect_habitat_params()}},
not hard-coded. Two fixes join the same habitat when reachable through hops
no larger than \code{eps}.}

\item{minPts}{Minimum number of points within \code{eps} for a core point.
\strong{Required, no default} -- typically from \code{\link[=detect_habitat_params]{detect_habitat_params()}}. Acts
as a uniform density / noise floor; interpretable as a minimum residence
time given the sampling interval.}

\item{drop_noise}{Logical; if \code{TRUE} (default) noise points (DBSCAN cluster
\code{0}) are dropped from the returned object.}
}
\value{
The input sf object with an added integer column \code{cluster_id}
giving the per-individual habitat id (\verb{1..k}; \code{0} = noise, retained only
when \code{drop_noise = FALSE}).
}
\description{
Clusters stationary tracking fixes into temporary habitats using standard
DBSCAN (Ester et al. 1996), applied independently to each individual. Unlike
density-hierarchy methods (HDBSCAN), both parameters here are fixed and
interpretable rather than auto-extracted:
}
\details{
\itemize{
\item \code{eps} is a fixed, biologically meaningful spatial scale -- the
species-typical maximum within-habitat movement, obtained from the
population step-length distribution (the upper mode of local, non-flight
steps). Occupancy at this scale is self-similar (scale-free), so a single
explicit scale is more defensible than a brittle auto-selected optimum.
\item \code{minPts} is a unified noise / minimum-duration floor -- the knee of the
cluster-count vs \code{minPts} curve, which for the Black-tailed Godwit data
coincides with a minimum residence of ~3 days at a 6-hour sampling
interval (\code{minPts = 12}).
}

Time is deliberately excluded: habitats are defined purely from spatial
occupancy, leaving temporal use to be analysed downstream.
}
\examples{
\dontrun{
stationary <- seg[seg$cluster_type \%in\% c("LL", "LH"), ]
## 1. derive thresholds from the data (never hard-code them)
p <- detect_habitat_params(stationary, individual_col = "Individual",
                           time_col = "Time")
## 2. run transparent per-individual DBSCAN with the derived thresholds
hab <- dbscan_habitats(stationary, individual_col = "Individual",
                       eps = p$eps, minPts = p$minPts)
tapply(hab$cluster_id, hab$Individual, function(z) length(unique(z)))
}
}
\references{
Ester, M., Kriegel, H.-P., Sander, J., & Xu, X. (1996).
A density-based algorithm for discovering clusters in large spatial
databases with noise. \emph{Proc. KDD-96}, 226-231.
}
\seealso{
\code{\link[=detect_habitat_params]{detect_habitat_params()}} to derive \code{eps} and \code{minPts} from data.
}
