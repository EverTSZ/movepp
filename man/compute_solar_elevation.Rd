% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian.R
\name{compute_solar_elevation}
\alias{compute_solar_elevation}
\title{Compute Solar Elevation Angle at Each Tracking Point}
\usage{
compute_solar_elevation(points, time_col, verbose = TRUE)
}
\arguments{
\item{points}{An \code{sf} POINT object.}

\item{time_col}{Column name (string) of a POSIXct/POSIXt time
column. Must include time zone information for correct local-time
computation.}

\item{verbose}{Logical; print progress.}
}
\value{
The input \code{sf} with a new \code{solar_elevation} numeric column
in degrees (positive = above horizon, negative = below).
}
\description{
Calculates the solar elevation angle (in degrees, with negative
values below the horizon) for each tracking point, given its
coordinates and timestamp. Used downstream by \code{\link[=classify_circadian]{classify_circadian()}}
to partition activity into day, twilight, and night periods.
}
\examples{
\dontrun{
demo <- make_demo_track(n_individuals = 1, n_points = 100)
demo <- compute_solar_elevation(demo, time_col = "time")
hist(demo$solar_elevation)
}

}
\references{
Whitener, B. (2025). \emph{Pysolar: Python Library for Solar Position
Calculations}. The R equivalent computation is performed by the
\code{suncalc} package (Thieurmel & Elmarhraoui).
}
