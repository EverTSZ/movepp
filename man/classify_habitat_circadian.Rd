% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian.R
\name{classify_habitat_circadian}
\alias{classify_habitat_circadian}
\title{Type each temporary habitat by its dominant diel period}
\usage{
classify_habitat_circadian(
  points,
  circadian_col = "circadian",
  cluster_col = "cluster_id",
  individual_col = NULL,
  majority = 0.5,
  periods = c("Day", "Twilight", "Night"),
  verbose = TRUE
)
}
\arguments{
\item{points}{An \code{sf} object carrying a per-fix circadian label
(typically the \code{circadian} column from \code{\link[=classify_circadian]{classify_circadian()}}) and a
temporary-habitat cluster id (the \code{cluster_id} from
\code{\link[=dbscan_habitats]{dbscan_habitats()}}).}

\item{circadian_col}{Per-fix diel-label column (default \code{"circadian"}).}

\item{cluster_col}{Temporary-habitat cluster-id column (default
\code{"cluster_id"}).}

\item{individual_col}{Individual-id column, or \code{NULL} (default) to treat
\code{cluster_col} as globally unique. When supplied, habitats are typed
within each \verb{(individual, cluster)} pair.}

\item{majority}{Numeric in [0, 1]; the minimum fraction the dominant
period must reach for the habitat to take its label rather than
\code{"Mixed"} (default 0.5).}

\item{periods}{Character vector of diel levels to tabulate and rank
(default \code{c("Day", "Twilight", "Night")}).}

\item{verbose}{Logical; print the resulting habitat-type counts.}
}
\value{
The input \code{sf} with an added factor column \code{habitat_circadian}
(levels \code{periods} plus \code{"Mixed"}), constant within each habitat. A
one-row-per-habitat summary -- cluster id, fix count, per-period
proportions, and assigned type -- is attached as the attribute
\code{"habitat_circadian"}.
}
\description{
Aggregates the per-fix circadian labels from \code{\link[=classify_circadian]{classify_circadian()}} to
the temporary-habitat (cluster) level. Each habitat is assigned the
diel period holding a majority (greater than \code{majority}) of its fixes,
or \code{"Mixed"} when no period does. The per-fix day/twilight/night label
is only an intermediate; the habitat-level functional type -- a daytime
foraging site, a night roost, a twilight transit area -- is the
ecological object this returns.
}
\examples{
\dontrun{
pts <- compute_solar_elevation(habitat_pts, time_col = "time")
pts <- classify_circadian(pts)
pts <- classify_habitat_circadian(pts, individual_col = "individual")
attr(pts, "habitat_circadian")
}

}
\seealso{
\code{\link[=classify_circadian]{classify_circadian()}} for the per-fix labels this aggregates.
}
