% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_phases.R
\name{annotate_phases}
\alias{annotate_phases}
\title{Interactively annotate phases on a behaviour barcode}
\usage{
annotate_phases(
  points,
  time_col = "time",
  rank_col = "cluster_rank",
  individual_col = NULL,
  phase_col = NULL,
  basemap = c("light", "natural", "gray"),
  phases = c("Wintering", "Stopover", "Breeding"),
  viewer = c("browser", "pane", "dialog")
)
}
\arguments{
\item{points}{An \code{sf} POINT object with at least \code{time_col} and \code{rank_col}.}

\item{time_col}{POSIXct/Date column name (string).}

\item{rank_col}{Discrete habitat-rank column (default \code{"cluster_rank"}).}

\item{individual_col}{Optional individual-id column; adds a selector.
\code{NULL} (default) shows all points.}

\item{phase_col}{Optional column of starting phase labels to seed the
annotation (e.g. \code{"phase"} from \code{\link[=classify_phases]{classify_phases()}}); the gadget opens
with these instead of all-"unassigned". \code{NULL} (default) starts blank.}

\item{basemap}{Coastline-map colour preset: \code{"light"} (default),
\code{"natural"}, or \code{"gray"}. Coastlines, rivers, lakes, land and ocean are
drawn; country borders never are.}

\item{phases}{Preset phase labels (default \code{c("Wintering","Stopover","Breeding")}).}

\item{viewer}{\code{"browser"} (default), \code{"pane"}, or \code{"dialog"}.}
}
\value{
The input \code{sf} with an added factor column \code{phase_manual}, original
row order; \code{NULL} if cancelled.
}
\description{
Launches a Shiny gadget with a habitat-occupancy \emph{behaviour barcode}
(time x habitat \code{rank_col}) linked to a clean coastline map (no country
borders). Box-select on EITHER panel; the selection is ringed in black on
both and can be assigned a life-history phase or a free-text custom label.
With \code{individual_col} set, a dropdown restricts the view to one individual.
Returns the input with an added \code{phase_manual} column.
}
\details{
If \code{phase_col} is supplied, the gadget \strong{opens with those labels as the
starting classification} (e.g. the output of \code{\link[=classify_phases]{classify_phases()}}), so
only the mis-assigned points need correcting rather than annotating from
scratch. Because the barcode is organised by habitat rank, box-selecting a
whole rank-row over a time span selects an entire habitat -- so corrections
can be made per habitat (cluster), matching the cluster-level classification output.
}
\section{Interaction}{

\itemize{
\item With \code{individual_col}, pick a bird from the \strong{Individual} dropdown
(or \code{(all)}); switching clears the current selection.
\item Box-select on the \strong{barcode} (drag) or \strong{map} (modebar box/lasso);
selected points are ringed in black on BOTH panels.
\item Click a preset phase button, or type a custom label and \emph{Apply
custom}; \emph{Unassign} clears labels.
\item Toggle \strong{Colour by} between habitat rank and assigned label.
\item \emph{Done} returns the annotated data; \emph{Cancel} aborts.
}

Requires \pkg{shiny}, \pkg{miniUI} and \pkg{plotly} (Suggests).
}

\seealso{
\code{\link[=classify_phases]{classify_phases()}} for the automatic classification this can
refine, \code{\link[=dbscan_habitats]{dbscan_habitats()}} for producing the habitat ranks.
}
