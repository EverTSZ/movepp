% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo-data.R
\name{make_demo_track}
\alias{make_demo_track}
\title{Generate a synthetic demo movement track}
\usage{
make_demo_track(n_individuals = 2L, n_points = 200L, seed = 1L)
}
\arguments{
\item{n_individuals}{Integer; number of individuals to simulate
(default 2).}

\item{n_points}{Integer; total number of points per individual
(default 200).}

\item{seed}{Integer; random seed for reproducibility (default 1).}
}
\value{
An \code{sf} POINT object in EPSG:4326 with columns:
\itemize{
\item \code{individual}: character (e.g., \code{"ind_01"}).
\item \code{time}: POSIXct, hourly timestamps starting 2023-01-01.
\item \code{speed}: numeric, m/s.
\item \code{lon}, \code{lat}: numeric coordinates (also retained as columns).
}
}
\description{
Creates a synthetic \code{sf} POINT object with mixed migration and
stationary behaviour for one or more simulated individuals. Each
individual visits \strong{two stationary sites} (mimicking wintering and
breeding grounds for a migratory bird), connected by a migration
trail. Each stationary site is a tight cluster (sd ~500 m),
providing the spatial-occupancy structure that \code{\link[=dbscan_habitats]{dbscan_habitats()}}
delineates at a data-derived scale.
}
\details{
Per individual, the breakdown is roughly:
\itemize{
\item 35\% of points: wintering site (tight cluster at origin).
\item 35\% of points: breeding site (tight cluster, +5 deg lon,
+8 deg lat from origin).
\item 30\% of points: migration trail connecting the two sites
(high-speed, spatially spread).
}
Used in examples, vignettes, and unit tests.
}
\examples{
demo <- make_demo_track()
plot(demo["speed"])

}
